services:
  postgres:
    image: postgres:16
    container_name: fraud-postgres
    environment:
      POSTGRES_USER: fraud
      POSTGRES_PASSWORD: fraud
      POSTGRES_DB: fraud
    ports:
      - "5433:5432"
    volumes:
      - fraud_pg_data:/var/lib/postgresql/data

volumes:
  fraud_pg_data:
